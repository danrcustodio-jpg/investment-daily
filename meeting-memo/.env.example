# Required for transcription + summarization
OPENAI_API_KEY=sk-...

# Optional overrides
OPENAI_TRANSCRIBE_MODEL=whisper-1
OPENAI_SUMMARY_MODEL=gpt-4o-mini
# Bind (default 8766 so it does not clash with Investment Daily dashboard 5050)
PORT=8766
