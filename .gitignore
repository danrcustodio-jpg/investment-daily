# Credentials — never commit these
.env
*.env
schwab_token.json

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Logs — generated locally, not needed in repo
*.log
logs/

# Sensitive local files
.ngrok_token
.ngrok_url
ngrok.exe

# Windows
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# NOTE: alert_state.json, sim_portfolio.json, and contract_state.json are
# intentionally tracked by git so state persists between GitHub Actions runs.

# Runtime cache files
data/cot_positioning_cache.json

# Smoke test inspection artifacts (regenerated on each run)
tests/_smoke_*.out.*
